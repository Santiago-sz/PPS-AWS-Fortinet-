# Dependencias de desarrollo (tests, lint, análisis estático de IaC).
# No se instalan en el paquete de despliegue de la Lambda — solo se usan
# localmente / en CI para validar el código antes de subirlo.
pytest==8.3.5
pytest-mock==3.14.0
ruff==0.11.2
checkov==3.2.379
moto[s3,secretsmanager]==5.1.4
